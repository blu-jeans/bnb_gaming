# 泡泡堂家族比赛积分竞猜系统 - 项目背景与开发上下文 (CONTEXT)

本文件提炼了该项目当前的所有开发背景、架构设计、技术实现以及最新修改记录，以便在不同设备上（如回家后）导入给 AI 助手直接继续开发。

---

## 1. 🎯 项目背景与业务逻辑
- **项目定位**：一款专为泡泡堂家族比赛设计的积分竞猜与对局管理系统，由主持人（庄家）在现场进行实时操作。
- **核心角色**：
  1. **主持人（庄家）**：拥有独立账户（置顶展示），不参与累计积分统计，仅记录每次比赛（当天）的实时盈亏。下一次新比赛开始时清零重新统计。
  2. **成员大池**：存放所有参与投注的家族成员账号。支持在输入框内输入名字并敲击回车直接物理添加。点击药丸标签右侧的 `×` 物理删除。
  3. **参赛选手池**：存放当次比赛实际使用的游戏账号（内置预设：`别了七月`、`淡若衣长`、`倾城初遇`、`Mrˇ戀°`、`君诺寒`、`挨个弄死`）。
- **投注机制**：
  - 双击成员大池中的标签，或者将成员拖拽到红队/绿队投注区，自动弹出下注输入框。
  - 下注金额必须是 **5 的倍数**，且不能超过界面设定的投注上限。
  - 红/绿两队各最多只能有 **2 名参赛选手**，选手通过从“选手大池”中拖拽至红绿两队的“选手区”来指定。
- **结算与积分公式**：
  - 结算时，根据红绿两队比分与胜负方进行结算。
  - 输家输掉全部投注积分。
  - 赢家按其投注比例瓜分输家的积分池。
  - 庄家（主持人）承担输赢的差额溢出（即积分抽水或赔付盈亏），保证每局总积分为零和博弈。

---

## 2. 🛠️ 技术栈与架构实现
- **开发语言与框架**：Python 3.x + PyQt6
- **数据库**：SQLite (`bnb_guessing.db`)，在 `src/database/repository.py` 进行数据读写与 DDL 平滑热升级。
- **数据持久化变更**：`rounds` 和 `match_history` 表已加入 `bet_details` 字段以持久化存储每轮投注的具体明细（例如：“棉花(红队20分)”），并支持 Excel 导出。

---

## 3. 🚨 最近核心技术突破（重点）
### 📌 彻底解决 IconMode 下的拖拽死锁与字符截断 Bug
- **之前的问题**：
  1. 之前使用 `QListWidget` 的 `IconMode` 进行标签平铺，但 `IconMode` 底层在 Windows 下会强行拦截自定义鼠标事件导致拖拽时高频重入，光标永久锁死为“红色禁止符号”。
  2. 宽度裕量计算偏小，导致名字后面拼装的删除符 ` ×` 被强制截断，显示为极其难看的省略号 `...`。
- **当前的终极方案**：
  1. **流式 ListMode**：全面抛弃了存在事件黑洞的 `IconMode`，改用极其稳定的原生 **`ListMode`**，并通过以下属性设置实现完全相同的流式换行排版：
     ```python
     self.setViewMode(QListWidget.ViewMode.ListMode)
     self.setFlow(QListWidget.Flow.LeftToRight) # 从左往右横向排列
     self.setWrapping(True)                     # 自动换行
     self.setResizeMode(QListWidget.ResizeMode.Adjust)
     ```
  2. **原生拖拽机制**：恢复了原生 `startDrag(supportedActions)` 的虚函数重载，由于是纯净的 `ListMode` 流式排版，PyQt 原生 C++ 拖放事件链被完美打通，**拖拽无比丝滑，不再报错红叉**。
  3. **字符完整展开**：将动态测算文本物理像素宽度时的余量从 `+24` 增加到了 **`+45`**，在任何分辨率大屏/小屏下，文字和删除叉都能 100% 完整清爽地展开，绝无省略号。

---

## 4. 🚀 下一步计划与优化方向
1. **下注交互体验测试**：回家导入上下文后，在本地拉取代码并运行 `python main.py`，测试从大池拖拽成员/选手到红绿队下注/指定选手的流畅度。
2. **多屏幕分辨率自适应微调**：进一步观察在不同 DPI 的屏幕下（特别是 4K 大显示器或小屏幕笔记本），窗口顶部及右下角“本届赛事盈亏榜”（庄家置顶黄金色高亮 `👑`）的自适应布局。
3. **结算流程验证**：跑一两局模拟对局，点击“结算本轮”，验证结算明细弹窗的滚动区域（已加 `QScrollArea` 避免挤压）及 Excel 导出的正确性。
